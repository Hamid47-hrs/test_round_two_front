const DescriptionField = ({ description }) => {
  return (
    <div className="container">
      <ul className="data-list">
        <li className="data_item">{description}</li>
      </ul>
    </div>
  );
};

export default DescriptionField;
